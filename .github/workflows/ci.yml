name: CI

# This repository is archived. CI is disabled; development continues in
# https://github.com/harborclient/harborclient (packages/http).

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Repository archived
        run: |
          echo "http development moved to harborclient/harborclient packages/http."
          echo "See https://github.com/harborclient/harborclient"
