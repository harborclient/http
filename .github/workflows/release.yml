name: Release

# This repository is archived. Releases are published from
# https://github.com/harborclient/harborclient via http-release.yml (http-v* tags).

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Repository archived
        run: |
          echo "http releases moved to harborclient/harborclient."
          echo "Run: pnpm release:http"
          exit 1
